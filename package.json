{
  "scripts": {
    "todos": "parcel todos/index.html --no-hmr",
    "bench": "parcel bench/index.html --no-hmr",
    "clean": "rm -rf {todos,bench}/{dist,.cache,target}"
  },
  "dependencies": {
    "parcel-bundler": "^1.5.1",
    "parcel-plugin-cargo-web": "^0.1.0"
  }
}
